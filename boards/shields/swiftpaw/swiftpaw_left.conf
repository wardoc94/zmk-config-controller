CONFIG_ADC=y

# UNVERIFIED - best guess at the Kconfig symbol for
# letmegobacktosleep/zmk-keyboard-joystick-wasd. Check its Kconfig file and
# correct this symbol name if the build complains it's unused/unknown.
CONFIG_ZMK_KEYBOARD_JOYSTICK_WASD=y
